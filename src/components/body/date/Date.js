import './Date.css';
import React, { useEffect, useState } from "react";

function Date() {
    // const [dateState, setDateState] = useState(new Date());
    // useEffect(() => {
    //        setInterval(() => setDateState(new Date()), 30000);
    // }, []);

    // return(
    //     <div className='date-container'>
    //         <p className='date'> {' '}
    // //           {dateState.toLocaleDateString('en-GB', {
    //              day: 'numeric', month: 'short', year: 'numeric',})} | Time Zone here!</p>
    //     </div>
    // );

    // const [dateState, setDateState] = useState(new Date());
    // useEffect(() => {
    //        setInterval(() => setDateState(new Date()), 30000);
    // }, []);

    // return (
    //     <div className="App">
    //         <p>
    //           {' '}
    //           {dateState.toLocaleDateString('en-GB', {
    //              day: 'numeric',
    //              month: 'short',
    //              year: 'numeric',
    //           })}
    //         </p>
    //         <p>
    //          {dateState.toLocaleString('en-US', {
    //             hour: 'numeric',
    //             minute: 'numeric',
    //             second: 'numeric',
    //             hour12: false,
    //         })}
    //         </p>
    //     </div>
    // );
}

export default Date;